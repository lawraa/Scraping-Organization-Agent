# Copy to .env and fill in your key
GEMINI_API_KEY=API_KEY_HERE

# Optional: model name (default in code if unset)
GEMINI_MODEL=gemini-2.5-flash

# How many index pages to scan on each run (ignored if --all)
PAGES_TO_SCAN=2

# Crawl politeness
REQUEST_DELAY=0